from felles_funksjoner import trykk

def objekt_liste (gjenstand):
    if gjenstand == "coal":
        #objekt_farge = (45, 45, 45)

        trykk(3790, 550)
        trykk(3680)
        return 5

    elif gjenstand == "diamond":

        trykk(3950, 550)
        trykk(3680)
        return 30